<div align="center">

# Function Utilities

<sub>🔧 Build Your Own · **7 questions**</sub>

<a href="README.md">⬅ Bank index</a> &nbsp;·&nbsp; <a href="../README.md">Build Your Own</a> &nbsp;·&nbsp; <a href="../../README.md">Home</a>

</div>

> 🟢 Easy · 🟡 Medium · 🔴 Hard · prompts only — work the answers using the section guides.

---

- 🟡 Implement `debounce(fn, wait)` — then add a `{ leading, trailing }` option.
- 🟡 Implement `throttle(fn, wait)` and explain how it differs from debounce.
- 🟢 Implement `once(fn)` so a function runs at most one time.
- 🟡 Implement `memoize(fn)` with a configurable cache key resolver.
- 🔴 Implement `curry(fn)` that works for any arity.
- 🔴 Implement `Function.prototype.bind` from scratch, handling `new`.
- 🟡 Implement a `pipe`/`compose` function combinator.

---

_Part of the [Build Your Own question bank](README.md) · prompts curated for interview practice._
