<div align="center">

# Promises & Async

<sub>🔧 Build Your Own · **7 questions**</sub>

<a href="README.md">⬅ Bank index</a> &nbsp;·&nbsp; <a href="../README.md">Build Your Own</a> &nbsp;·&nbsp; <a href="../../README.md">Home</a>

</div>

> 🟢 Easy · 🟡 Medium · 🔴 Hard · prompts only — work the answers using the section guides.

---

- 🟡 Implement `Promise.all` from scratch.
- 🟡 Implement `Promise.allSettled`.
- 🟡 Implement `Promise.race` and `Promise.any`.
- 🟡 Implement a `retry(fn, times)` helper with exponential backoff.
- 🟢 Implement a promise-based `sleep(ms)` and a `timeout(promise, ms)` wrapper.
- 🔴 Implement a concurrency limiter that runs N async tasks at a time.
- 🔴 Implement a minimal Promise (`MyPromise`) with `then`, resolve, and reject.

---

_Part of the [Build Your Own question bank](README.md) · prompts curated for interview practice._
